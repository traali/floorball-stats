# Chapter Ritual Workflow
Convened whenever a cross-repo contract amendment or major RFC is proposed.
1. Define proposed interface change in `contracts/index.ts`.
2. Ensure backward compatibility: all new fields optional.
3. Validate with all 5 monasteries before merging.
